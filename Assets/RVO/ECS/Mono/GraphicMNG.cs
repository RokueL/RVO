using UnityEngine;

public class GraphicMNG : MonoBehaviour
{
    public Material Material;
    public Mesh Mesh;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
